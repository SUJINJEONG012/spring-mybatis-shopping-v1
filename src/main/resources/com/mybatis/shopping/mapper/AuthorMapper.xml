<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  <mapper namespace="com.mybatis.shopping.mapper.AuthorMapper">
  
  <!-- 작가 등록  -->
  <insert id="authorEnroll">
   insert into author(authName, nationId, authorIntro) values (#{authName}, #{nationId}, #{authorIntro})
  </insert>
  
  <!-- 작가 목록 -->
  <select id="authorGetList" resultType="com.mybatis.shopping.model.AuthorVo">

  select authorid, authname, nationid, regdate, updatedate 
  from author
  <if test="keyword != null">
  where authName like concat('%', #{keyword}, '%')
  </if>
  order by authorId desc 
  limit #{skip}, #{amount}
  </select> 
  
  </mapper>
  